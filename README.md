# HDF5 互動式命令列瀏覽器 (HDF5 Interactive CLI Browser)

這是一個簡單的 Python 腳本，提供一個互動式的命令列介面 (Command-Line Interface, CLI)，用於瀏覽和檢查 HDF5 (`.h5`, `.hdf5`) 檔案的內容。

對於經常需要快速檢查 HDF5 檔案結構或預覽其中數據的科學家和工程師來說，這個工具非常有用，無需開啟 HDFView 等圖形介面軟體。

## 🚀 功能特性

  * **類 Unix/Linux 導覽**：使用 `ls`, `cd`, `pwd` 等熟悉的指令在 HDF5 的群組 (Groups) 結構中導覽。
  * **數據即時預覽**：使用 `cat` 指令查看資料集 (Datasets) 的詳細資訊 (shape, dtype) 和數值內容。
  * **屬性查看**：使用 `attrs` 指令檢查群組或資料集的屬性 (Attributes)。
  * **支援空格**：完整支援名稱中包含空格的群組和資料集。
  * **輕量且單一檔案**：僅依賴 `h5py`，所有功能都在一個 Python 腳本中。

## 🛠️ 安裝需求

本腳本僅依賴 `h5py` 函式庫。

您可以使用 pip 來安裝它：

```bash
pip install h5py
```

## 🏃 如何使用

將腳本 (例如 `browse_h5.py`) 下載到您的電腦上，然後在終端機中執行：

```bash
python browse_h5.py /path/to/your_file.h5
```

成功開啟檔案後，您會看到一個提示符號，顯示您目前所在的檔案和 HDF5 路徑：

```
成功開啟檔案: your_file.h5
輸入 'help' 查看所有指令。
[your_file.h5:/]> 
```

## 📖 指令列表

以下是瀏覽器中可用的所有指令：

| 指令 | 範例 | 描述 |
| :--- | :--- | :--- |
| `ls` | `ls` | 列出目前層級 (Group) 下的所有子群組和資料集。 |
| `cd <path>` | `cd my_group` <br> `cd My Experiment Data` <br> `cd ..` <br> `cd /` | 切換目錄。<br> - `cd group_name`：進入指定的子群組。<br> - `cd ..`：回到上一層。<br> - `cd /`：回到根目錄。 |
| `cat <dataset>`| `cat my_data` <br> `cat S21 values` | 瀏覽 (concatenate) 資料集的內容。將會顯示該資料集的 Shape, Dtype, Attributes 以及數值預覽。 |
| `attrs [item]` | `attrs` <br> `attrs my_data` <br> `attrs My Group` | 顯示屬性。<br> - `attrs`：顯示**目前群組**的屬性。<br> - `attrs item_name`：顯示目前層級下，指定項目 (群組或資料集) 的屬性。 |
| `pwd` | `pwd` | 顯示目前所在的絕對路徑 (Print Working Directory)。 |
| `help` | `help` | 顯示輔助說明，列出所有可用指令。 |
| `exit` / `quit` | `exit` | 離開 HDF5 瀏覽器並關閉檔案。 |
